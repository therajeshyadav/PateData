# Neon PostgreSQL connection string
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Set to 1 to enable test mode for deterministic time testing
TEST_MODE=1

# Base URL for generating paste URLs (optional - auto-detected on Vercel)
BASE_URL=
